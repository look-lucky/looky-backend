services:
  # 1. MySQL 컨테이너
  looky-db:
    image: mysql:8.0
    container_name: looky-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: "0000"
      MYSQL_DATABASE: looky
    ports:
      - "3308:3306"
    volumes:
      - ./db_data:/var/lib/mysql

  # 2. Redis 컨테이너
  looky-redis:
    image: redis:alpine
    container_name: looky-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - ./redis_data:/data
